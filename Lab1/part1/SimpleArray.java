class SimpleArray
{
   public static int [] squareAll(int values[])
   {
      
      int [] newValues = new int[value.length];  // This allocates an array of integers.

      for(int i = 0; i<value.length; i++){
         newValues[i] = values[i];
      }
      return newValues;
   }
}
